<div class="overlay">
    <div class="square">
            <div>Congratulazioni per aver completato il timer!</div>
            <button class="btn btn-primary mt-auto mb-3" @onclick="ButtonChecked">OK</button>
        </div>
    </div>

<style>
    .overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        display: flex;
        justify-content: center;
        align-items: center;
        background-color: rgba(0, 0, 0, 0.5); /* Overlay trasparente */
        z-index: 1000; /* Assicurati che sia sopra gli altri elementi */
    }

    .square {
        width: 200px;
        height: 200px;
        background-color: white;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        align-items: center;
        border: 2px solid black;
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
        text-align: center;
        padding: 20px;
    }
</style>

@code {
    [Parameter]
    public EventCallback OnButtonChecked { get; set; }

    private void ButtonChecked()    
    {
        OnButtonChecked.InvokeAsync();
    }
}